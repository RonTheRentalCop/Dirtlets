Project Progress and Timeline for Success

- To Do - 
[x] Created Github Repository
[x] Log Book Made
[x] Obtain a 3d Printer
[x] Finish Parts List
[x] Verify Parts Compatibility
[] 

Thursday September 3rd
- Set up the project foundation for Dirtlets.
- Created the GitHub repository and connected the project to a version-controlled workspace.
- Created the Log Book and started documenting the project timeline and milestones.
- Reviewed the project concept and confirmed the core goal: a swarm of small robots for lunar/planetary exploration and mapping.
- Added the initial project checklist in the README to track major work items.

Friday September 4th
- Today’s goals:
  - Finish the parts list for the first prototype.
  - Verify compatibility between the selected 3D printer, motors, electronics, and structural components.
  - Decide which parts are required now versus optional for later iterations.
  - Start listing exact parts, sources, and estimated costs.
  - Review whether the current design direction matches the swarm proof-of-concept goal.

Over the Weekend I finished up the parts list and fixed the 3d printer. Should order items soon. Finish paperwork to make absolutly sure I want to do it this way.

Thursday September 10th
- Decided that the robots will stream camera video and sensor data to a central computer instead of performing the main computations onboard each robot.
- Reason for this change: Raspberry Pi computers and similar onboard computing hardware are too expensive to purchase for every robot. The existing ESP32 boards can handle camera streaming, motor control, and basic sensor communication at a lower cost.
- The central computer will perform the expensive operations, including object detection, distance processing, localization, map building, path planning, and combining data from multiple robots in real time.
- The robots will receive movement commands from the central computer. Each robot should still have a local emergency-stop behavior if the connection is lost, so a communication failure does not leave the motors running.
- The first mapping goal is a 2D or 2.5D map using the RPLIDAR, camera video, wheel encoders, and the MPU-650. A full 3D map can be investigated later if the project requires additional depth hardware or a lidar scanning mechanism.
- Possible future deployment concept: a landing craft could act as the central relay and computing station. It could receive signals and sensor data from the deployed robots, stream or compile their data in real time, and send the combined results back to the main base or operator.
- This landing-craft concept is currently a possible future architecture, not a required part of the first prototype.

- Created `Build-Camp.md` with a simple flowchart showing the planned prototype build order, from confirming parts through the first mapping trial and design improvements.
- Improved `Build-Camp.md` by separating the flowchart into build, electronics, and testing phases, adding color-coded styling, and making the repair loop easier to follow. The underlying build plan was not changed.
- Added a robot control-loop flowchart to `Build-Camp.md` showing the ESP32-CAM stream, Wi-Fi connection, central computer processing, movement decision, motor ESP32 commands, and feedback back to the camera.
- Refined both flowcharts with a professional technical-document style: graphite, steel blue, muted teal, and limited amber for decisions. Removed the pastel and purple styling to improve readability and visual hierarchy.
- Enlarged the Robot Control Loop diagram with larger text, more spacing, and added diagram padding so the operating cycle is easier to inspect.
- Object recognition will use an established image-recognition library running on the central computer. The project will not build an object-recognition system from scratch; its code will connect the library to the incoming ESP32-CAM video stream and use the detected objects to make movement decisions.

- Repaired `Build-Camp.md` after adding the robot design options. Placed the three option images in a single table row and removed the accidental indentation around the Mermaid build chart so it renders as a diagram again.
- Rebuilt the top build flowchart around the current parts list. Added separate preparation, drivetrain, power, control-electronics, sensor, and test phases, including the RPLIDAR C1, MPU-650, two ESP32 boards, ESP32-CAM, UBEC, motor driver, LiPo, JGY 370 motors, chassis, and central-computer testing.

Friday September 11th
- Cleaned up planning placeholders in `Object_Detection_Test.py` so the script can run while object recognition is still being designed.
- Added a temporary `detect_objects()` function that returns an empty list until the YOLO library is connected.
- Added valid placeholder bodies to the unfinished speaker and rage-mode functions.
- Moved the detection placeholder above the camera loop and removed references to undefined planning variables.

- Replaced the crowded top build chart with a cleaner four-stage architecture covering the mechanical platform, power and drive, control and sensing, and system verification.
- Added an “Assignment and Rationale” section to `README.md` explaining the swarm concept, lidar mapping, Health Score leadership, central-computer processing, and the cost advantage of ESP32-based rovers.
- Reworked the top flowchart to connect the complete exploration cycle: swarm sensing, central analysis, Health Score leadership, candidate-area selection, safe navigation, and map updates. Changed the remaining dark flowchart node to a white professional style.
- Created `Assignment-and-Rationale.md` with a grammatically revised version of the project proposal and its supporting rationale.
- Expanded `Assignment-and-Rationale.md` to answer the full assessment rubric: existing engineering knowledge, defined technical concepts, impacted stakeholders, personal connection, knowledge gap, proposed approach, cohesive rationale, and five APA-style sources.
- Added `Core/Lidar-Testing.py`, a simple OpenCV test UI for the RPLIDAR C1. It connects through the installed `rplidarc1` package at 460800 baud, displays a live polar scan, reports point count and nearest distance, and shows connection errors.
- Confirmed the project `.venv` uses Python 3.14 and has `rplidarc1`, `pyserial`, and `opencv-python` installed. The system Python 3.9 is not suitable because the lidar package requires Python 3.10 or newer.
- Enlarged the top mission flowchart to match the bottom control-loop chart by using the same 18px text, spacing, and diagram padding settings.

Monday September 14th
- Tested the RPLIDAR C1 validation program using the project `.venv`.
- Confirmed that the Python environment and lidar test code compile and import correctly.
- Checked the Mac for a USB serial device and confirmed that no RPLIDAR, SLAMTEC, or USB-to-serial device appeared.
- Reconnected the board and checked the USB connection again, but the computer still could not detect it.
- Determined that the original RPLIDAR board was dead on arrival. Hardware testing is paused until the replacement board arrives.
- Recorded the next hardware-testing sequence: check for a new serial port, run `Core/Lidar-Testing.py`, confirm live scan points, and verify the nearest-distance reading.

Tuesday September 16th
- Investigated why local commits were not appearing in the GitHub profile contribution history.
- Found that earlier commits used the placeholder email `YOUR_GITHUB_NOREPLY_EMAIL`, which GitHub could not associate with the account.
- Configured the local repository to use the GitHub username `RonTheRentalCop` and the verified email `koniarisg27@stu.wlwv.k12.or.us` for future commits.
- Confirmed that the repository already points to `RonTheRentalCop/Dirtlets` on GitHub.
- Future changes must be committed with the corrected identity and pushed to the `main` branch before they can appear in the profile history. Older commits with the placeholder email will not automatically be associated with the profile.
- Current project status: software planning and lidar test code are ready, but live sensor validation is waiting on the replacement board.

Friday September 18th
- Planned the responsibilities for the two ESP32 boards. The ESP32-CAM will stream video and basic telemetry, while the second ESP32 will control the motors and read local sensors.
- Decided that the central computer will run the demanding tasks, including YOLO object detection, lidar processing, mapping, and navigation decisions. This keeps the rover hardware affordable and reduces the processing load on the ESP32 boards.
- Identified the first ESP32 development goals: upload a basic test program, confirm serial communication, test the motor driver with the wheels lifted, and read data from the MPU-650 and wheel encoders.
- Added a communication-safety requirement: the motor ESP32 must stop the motors automatically if commands from the central computer stop arriving.
- Planned a simple command interface for early testing using commands such as `STOP`, `FORWARD`, `REVERSE`, `LEFT`, and `RIGHT` before moving to a more advanced network protocol.
- Planned to test the ESP32-CAM separately by confirming the camera works, connecting it to the Wi-Fi network, and reading its video stream with OpenCV on the central computer.
- Established the first complete demonstration goal: the central computer sends a movement command, the motor ESP32 responds briefly, and the rover stops automatically when communication is interrupted.
- Fixed the Git synchronization problem by removing the scheduled build-log workflow, rebasing local work onto the remote branch, and pushing the corrected history to GitHub.
- Updated the Git author identity to use the GitHub no-reply address `182666786+RonTheRentalCop@users.noreply.github.com` so GitHub privacy protection accepts future pushes.
- Reviewed the tracked rover design and identified practical problems with using it for the first physical prototype. The current chassis is too small for the larger treads that would make land traversal easier, and the correct tread materials are not currently available.
- Decided to consider a pivot to a larger wheeled chassis for the first prototype. The tracked design is still being kept as the long-term planetary rover direction.
- The larger wheeled chassis should provide enough room for the electronics, make telemetry wiring easier to manage, allow safer separation between power and signal systems, and leave space for waterproofing and weatherproofing.
- Developed a future swarm concept based on smaller Mars-rover-style vehicles. The swarm will be autonomous but heterogeneous: the robots will cooperate on the same mission while using different hardware systems for different jobs.
- Planned for squads of about three robots to travel together. Possible roles include a lidar mapping rover, a soil-analysis rover, and a dedicated communication rover that relays telemetry between the squad and the central computer.
- Added other possible specialized roles, including an autonomous RGB-camera rover that records environmental data and a robotic-arm rover controlled from an operator's RGB-camera view. The arm could collect rocks or soil, clear obstacles, or help another rover get unstuck.
- Added a design explanation block to the project notes because the build direction is changing. I am keeping all of the older tracked-rover writing, but I may start with a larger wheeled rover that looks more like a smaller Mars rover.
- The reason for the change is that I did not have the right tread materials, the original chassis was too small for larger treads, and the system needs more space for electronics, telemetry, safe wire separation, waterproofing, and weatherproofing.
- The swarm will be autonomous but will use different robot types instead of identical copies. Squads of about three robots could include a soil-analysis rover, a communication rover, and a lidar mapping rover. A separate robotic-arm rover could collect samples, clear obstacles, or help another rover get unstuck through an operator's RGB-camera view.
- I added these notes to the build documentation so the changes are explained as a practical prototype decision and not mistaken for deleting the original tracked design.
- Added another reason for the design pivot: my 3D printer has been unreliable. It was a free printer from Facebook Marketplace that I repaired. The gears, tracks, and axles printed correctly at first, but then the parts started coming out jumbled and unusable.
- Tried to fix the printer for about three to four weeks, but there was very little improvement. Since I cannot reliably print the tread parts, the larger wheeled rover is a more realistic first prototype while the printer and tracked design are still being worked on.
- Current project status: the ESP32 software plan is ready to begin, while lidar testing remains paused until the replacement board arrives.

Imma stop updating it cuz its mad annoying yo

Thursday September 24th
- I switched the active ESP32 firmware over to MicroPython because that is what the boards already have installed and I do not know C yet.
- I removed the unused ESP-IDF C motor-controller project instead of keeping two different firmware paths around to confuse me.
- Set up the MicroPython motor controller for the ESP32-WROOM-32E and TB6612FNG motor driver.
- Added `main.py` to start the board, connect to Wi-Fi, receive UDP commands, control the motors, and stop the motors if commands stop arriving.
- Added `config.py` so I can change Wi-Fi, GPIO pins, UDP settings, motor timeout, and light pins without rewriting the rest of the program.
- Added `motor.py` for PWM motor control, `udp_control.py` for the UDP commands, and `lights.py` for the status light.
- Added a temporary setup Wi-Fi access point called `Dirtlets-Motor` so the board can still be tested before I put in normal Wi-Fi credentials.
- Installed `mpremote` in the project Python environment and used it to upload the MicroPython files to `/dev/cu.usbserial-1220` without erasing MicroPython.
- Added `Board_Test.py` to make the configured lights flash rapidly. It can be run directly from the Mac without replacing `main.py`.
- Added `requirements.txt` with the `mpremote` dependency and updated the ESP32 documentation with the actual upload and light-test commands.
- The current firmware is only on the motor-controller board. The ESP32-CAM still needs its exact board model and pin map confirmed before I write its MicroPython camera program.



Sources for photos 
Monocular Depth Estimation with Depth Anything V2
How do neural networks learn to estimate depth from 2D images?
https://towardsdatascience.com/monocular-depth-estimation-with-depth-anything-v2-54b6775abc9f/

